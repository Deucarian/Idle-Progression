# Offline Reward Example

This pure C# sample calculates one hour of offline production with an eight-hour catch-up cap and a recurring ten-minute cycle reward.

Call `OfflineRewardExample.CalculateOneHour()` from an EditMode test or application bootstrap, then inspect its currency reward bundle and elapsed-time result.
